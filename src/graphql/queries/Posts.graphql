query Posts($pagination: Pagination!) {
  posts(pagination: $pagination) {
    id
    creationTime
    title
    contents
    commentCount
    user {
      id
      nickname
    }
    group {
      id
      name
    }
  }
}
