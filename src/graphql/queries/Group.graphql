query Group($id: ID!) {
  group(id: $id) {
    id
    name
    description
    imageUrl
    isJoined
    leader {
      id
      nickname
    }
    newMembers {
      id
      nickname
      imageUrl
    }
  }
}
