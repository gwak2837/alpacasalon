query PostsByGroup($groupId: ID!) {
  postsByGroup(groupId: $groupId) {
    isJoined
    posts {
      id
      creationTime
      title
      contents
      commentCount
      user {
        id
        nickname
      }
    }
  }
}
