query ZoomReviews($zoomId: ID!, $pagination: Pagination!) {
  zoomReviews(zoomId: $zoomId, pagination: $pagination) {
    id
    creationTime
    contents
    writer {
      id
      nickname
      imageUrl
    }
  }
}
